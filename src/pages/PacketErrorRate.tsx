import { Card } from 'flowbite-react';

export default function PacketErrorRate() {
  return (
    <Card className="per-card">
      <h2 className="text-xl font-bold tracking-tight text-gray-900">Packet Error Rate calculator</h2>
      <p className="text-sm text-gray-500 dark:text-gray-400">Feature under development.</p>
    </Card>
  );
}
